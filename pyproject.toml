[project]
name = "erp_implementation"
authors = [
    { name = "Syvasoft Business Solutions", email = "info@syvasoft.com"}
]
description = "End-to-end ERP implementation project management app (v2), built on Frappe/ERPNext standard DocTypes."
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.dev-dependencies]

[tool.bench.frappe-dependencies]
frappe = ">=15.0.0,<17.0.0"
